import { readLatin1String } from "../../util.js";
export function _embind_register_emval(rawType) {
    console.log(`_embind_register_emval(${readLatin1String(this.instance, rawType)})`);
    debugger;
}
