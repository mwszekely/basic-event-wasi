import { readLatin1String } from "../../util.js";
export function _embind_register_void(rawType, name) {
    console.log(`_embind_register_void(${readLatin1String(this.instance, rawType)}, ${readLatin1String(this.instance, name)})`);
    debugger;
}
