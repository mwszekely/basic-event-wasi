import type { InstantiatedWasm } from "../wasm.js";


export function _embind_register_emval(this: InstantiatedWasm, typePtr: number): void {
    // TODO...
}

export function _emval_take_value(this: InstantiatedWasm, rawTypePtr: number, ptr: number): any {
    // TODO...
    return 0;
}
export function _emval_decref(this: InstantiatedWasm, handle: number): number {
    // TODO...
    return 0;
}
