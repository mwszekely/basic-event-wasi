import type { InstantiatedWasm } from "../wasm.js";


export function _embind_register_emval(this: InstantiatedWasm, _typePtr: number): void {
    // TODO...
}

export function _emval_take_value(this: InstantiatedWasm, _rawTypePtr: number, _ptr: number): unknown {
    // TODO...
    return 0;
}
export function _emval_decref(this: InstantiatedWasm, _handle: number): number {
    // TODO...
    return 0;
}
